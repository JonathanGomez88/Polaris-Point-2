import React from "react";

const Accounts = () => ( <
    div >
    <
    h1 > Account Page < /h1> <
    p >
    This is the page that is used
    for viewing users accounts.

    <
    /p> < /
    div >
);

export default Accounts;